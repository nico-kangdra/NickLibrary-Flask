apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nick-ingress
  # namespace: nick
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-cert
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - nick.nk.sytes.net
    secretName: nick-cert
  rules:
  - host: nick.nk.sytes.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nick-service
            port:
              number: 3000
